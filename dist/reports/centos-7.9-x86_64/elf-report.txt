# Layer 1: ELF Dependency Scan Report
# Platform: centos-7.9-x86_64
# Status: PROJECTED (no TXSQL binaries to scan)
# Date: 2026-07-22
# Generated by: projected from known MySQL 8.0 binary layout
#
# THIS REPORT IS A PROJECTION. Run inspect-elf.sh against actual
# TXSQL binaries to produce the real report.

==============================================================================
TXSQL ELF Dependency Detail Report (PROJECTED)
Platform: centos-7.9-x86_64
TXSQL:    /usr/lib/txsql/<version>/
==============================================================================

Total ELF files:           ~120 (projected from MySQL 8.0 standard install)
Total unique NEEDED libs:  ~25 (projected)
Total ELFs with not-found: UNKNOWN (requires real scan)
Total unique not-found:    UNKNOWN

--- All NEEDED libraries (deduplicated, projected) ---
libaio.so.1
libcrypt.so.1
libcrypto.so.1.1
libc.so.6
libcurl.so.4
libdl.so.2
libgcc_s.so.1
libjemalloc.so.2
liblber-2.4.so.2
libldap-2.4.so.2
liblz4.so.1
libm.so.6
libncurses.so.5
libnuma.so.1
libpthread.so.0
libresolv.so.2
librt.so.1
libsasl2.so.3
libssl.so.1.1
libstdc++.so.6
libtinfo.so.5
libtirpc.so.1
libz.so.1
libzstd.so.1

--- All RPATH/RUNPATH entries (projected) ---
mysqld:    $ORIGIN/../lib/private
mysql:     $ORIGIN/../lib/private
*.so:      $ORIGIN/../lib/private

--- Not-found libraries (will be populated by real scan) ---
STATUS: Run 'bash build/inspect-elf.sh --txsql-dir /usr/lib/txsql/current'
        on a CentOS 7.9 system with TXSQL installed to produce real results.

--- Per-ELF detail (projected file list) ---

Server binaries:
  mysqld              — main server daemon (~120-200 MB)
  mysqld-debug        — debug build (if compiled with -DWITH_DEBUG=ON)
  mysql_tzinfo_to_sql — timezone loader
  mysql_ssl_rsa_setup — SSL certificate generator

Client binaries:
  mysql               — interactive client
  mysqladmin          — admin utility
  mysqlbinlog         — binary log reader
  mysqlcheck          — table check/repair
  mysqldump           — logical backup
  mysqlimport         — data import
  mysqlpump           — parallel dump
  mysqlshow           — show databases/tables
  mysqlslap           — load testing
  mysqltest           — test framework (if built)
  mysql_config_editor — login path editor

Shared libraries:
  libmysqlclient.so   — C client library
  libmysqld.so        — embedded server library (if built)
  libmysqlservices.so — service layer

Plugins (projected, list depends on build configuration):
  auth_socket.so      — Unix socket auth
  auth_test_plugin.so — test auth
  component_keyring_file.so   — keyring
  component_validate_password.so — password validation
  ha_example.so       — example storage engine
  ha_innodb.so        — InnoDB (usually statically linked!)
  ha_myisam.so        — MyISAM
  ha_csv.so           — CSV
  ha_archive.so       — Archive
  ha_blackhole.so     — Blackhole
  ha_federated.so     — Federated
  ha_ndbcluster.so    — NDB Cluster (unlikely in TXSQL)
  ha_tokudb.so        — TokuDB (possibly in TXSQL)
  ha_rocksdb.so       — RocksDB (possibly in TXSQL)
  innodb_engine.so    — InnoDB additional
  libpluginmecab.so   — MeCab parser
  mypluglib.so        — example plugin
  rewrite_example.so  — rewrite plugin
  semisync_master.so  — semi-sync master
  semisync_slave.so   — semi-sync slave
  validate_password.so — password validation

Helper executables:
  ibd2sdi             — InnoDB SDI extractor
  innochecksum        — InnoDB checksum tool
  my_print_defaults   — config printer
  myisamchk           — MyISAM check
  myisamlog           — MyISAM log
  myisampack          — MyISAM pack
  myisam_ftdump       — MyISAM fulltext dump
  mysql_tzinfo_to_sql — timezone loader
  perror              — error code lookup
  replace             — string replace
  resolve_stack_dump  — stack trace resolver
  resolveip           — DNS resolver
  zlib_decompress     — zlib decompress

# =============================================================================
# VERIFICATION CHECKLIST (run on real TXSQL install)
# =============================================================================
# [ ] mysqld:         ldd → no "not found"
# [ ] mysql:          ldd → no "not found"
# [ ] mysqladmin:     ldd → no "not found"
# [ ] mysqldump:      ldd → no "not found"
# [ ] mysqlbinlog:    ldd → no "not found"
# [ ] mysqlcheck:     ldd → no "not found"
# [ ] ALL plugins:    ldd → no "not found"
# [ ] ALL *.so:       ldd → no "not found"
# [ ] libmysqlclient: ldd → no "not found"
# [ ] RPATH set:      readelf -d mysqld | grep RPATH → $ORIGIN/../lib/private
# [ ] Private libs resolved: ldd mysqld | grep private → all found
# =============================================================================
