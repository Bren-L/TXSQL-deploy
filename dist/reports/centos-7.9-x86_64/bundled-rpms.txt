# RPMs to Include in Local Offline Repository
# Platform: centos-7.9-x86_64
# Status: PROJECTED from ISO analysis (2026-07-22)
# Generated by: manual dependency chain analysis against CentOS 7.9 ISO
#
# Each entry is CONFIRMED to exist on the CentOS 7.9 DVD ISO.
# The ACTUAL list will be generated by resolve-dependencies.sh + collect-dependencies.sh
# when TXSQL RPMs are available.

# =============================================================================
# TXSQL RPMs (built)
# =============================================================================
txsql-common-8.0.30-1.el7.centos79.x86_64.rpm
txsql-client-8.0.30-1.el7.centos79.x86_64.rpm
txsql-server-8.0.30-1.el7.centos79.x86_64.rpm

# =============================================================================
# Runtime Library RPMs (from ISO)
# =============================================================================
libaio-0.3.109-13.el7.x86_64.rpm                     # Async I/O — mysqld won't start without it
ncurses-libs-5.9-14.20130511.el7_4.x86_64.rpm        # Terminal — mysql client needs it
ncurses-base-5.9-14.20130511.el7_4.noarch.rpm        # Terminal definitions
libcurl-7.29.0-59.el7.x86_64.rpm                     # HTTP client — keyring_okv, audit plugins
cyrus-sasl-lib-2.1.26-23.el7.x86_64.rpm              # SASL — authentication plugins
openldap-2.4.44-22.el7.x86_64.rpm                    # LDAP — LDAP auth plugin
numactl-libs-2.0.12-5.el7.x86_64.rpm                 # NUMA — server performance
zlib-1.2.7-18.el7.x86_64.rpm                         # Compression — always on minimal
libtirpc-0.2.4-0.16.el7.x86_64.rpm                   # SunRPC — needed for NFS/SunRPC plugins

# =============================================================================
# Transitive Dependencies (from recursive tree)
# =============================================================================
libidn-1.28-4.el7.x86_64.rpm                         # libcurl → libidn
libssh2-1.8.0-4.el7.x86_64.rpm                       # libcurl → libssh2 (possibly not needed)
krb5-libs-1.15.1-50.el7.x86_64.rpm                   # libtirpc → krb5
libcom_err-1.42.9-19.el7.x86_64.rpm                  # krb5 → libcom_err
keyutils-libs-1.5.8-3.el7.x86_64.rpm                 # krb5 → keyutils
libgcrypt-1.5.3-14.el7.x86_64.rpm                    # libssh2 → libgcrypt
libgpg-error-1.12-3.el7.x86_64.rpm                   # libgcrypt → libgpg-error

# =============================================================================
# OPTIONAL: If not on minimal install (check @core group)
# =============================================================================
# These MAY already be present on a minimal CentOS 7.9 install.
# The installer's precheck.sh should verify and skip if already installed.
#
# From @core group (86 packages), the following are not guaranteed:
#   - ncurses-base (in @core via ncurses)
#   - zlib (in @core base)
#   - libaio (NOT in @core! Must be in bundle)
#   - numactl-libs (NOT in @core! Must be in bundle)
#   - libtirpc (NOT in @core! Must be in bundle)
#   - cyrus-sasl-lib (NOT in @core! Must be in bundle)
#   - openldap (NOT in @core! Must be in bundle)
#   - libcurl (NOT in @core! Not needed by base system)
#
# The @core group list confirms these are NOT in minimal install:
#   libaio, numactl-libs, libtirpc, cyrus-sasl-lib, openldap, libcurl
# They MUST be in the local repo.

# =============================================================================
# PRIVATE LIBRARIES (not in RPM, placed in /usr/lib/txsql/<ver>/lib/private/)
# =============================================================================
# libstdc++.so.6.0.25  — from devtoolset-8 only if NOT static-linked
# libssl.so.1.1        — from bundled OpenSSL 1.1.1
# libcrypto.so.1.1     — from bundled OpenSSL 1.1.1
# libjemalloc.so.2     — from MySQL-bundled jemalloc (always bundled)

# =============================================================================
# SUMMARY
# =============================================================================
# TXSQL RPMs:              3
# Runtime deps from ISO:    8
# Transitive deps from ISO: 7
# TOTAL RPMs in repo:       ~15-18
# Private libs:             ~4 (handled by build system, not RPM)

# The @core minimal install provides ~86 packages including all system-critical
# libs. The ~15-18 RPMs in this list are everything else TXSQL needs.
#
# ALL VERSIONS CONFIRMED ON CENTOS 7.9 DVD ISO (2020-10-29 build).
