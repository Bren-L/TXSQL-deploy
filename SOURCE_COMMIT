# TXSQL Source Reference — STATUS: BUILD-VERIFIED (2026-07-23)
#
# TXSQL 8.0.30 source code: tarball extract, no .git metadata.
# Located at: /opt/TXSQL-8.0.30 on build host (192.168.44.151).
# Source was transferred from Windows workstation to CentOS 7.9 VM.
#
# TXSQL_BRANCH=unknown (no .git)
# TXSQL_COMMIT=unknown (no .git)
# TXSQL_COMMIT_DATE=unknown
# TXSQL_VERSION=8.0.30-txsql
# MYSQL_BASE_VERSION=8.0.30
# SOURCE_TARBALL=TXSQL-8.0.30.tar.gz
# SOURCE_FILES=117,155
#
# Build verified on CentOS 7.9 (192.168.44.151):
#   GCC 10.2.1 (devtoolset-10)
#   glibc 2.17
#   OpenSSL 3.4.1 (bundled in source)
#   Boost 1.77.0 (bundled in source)
#   cmake3 3.17.5 (EPEL)
#   patchelf (EPEL)
#   perl-IPC-Cmd (EPEL)
#
# For comparison, CentOS 7.9 baseline (from ISO):
#   glibc 2.17-317.el7 (build floor)
#   GCC 4.8.5-44.el7 (system default — must use devtoolset-10)
#   CMake 2.8.12.2-2.el7 (must use cmake3 from EPEL)
#   OpenSSL 1.0.2k-19.el7 (EOL — TXSQL bundles 3.4.1)
#   systemd 219-78.el7
