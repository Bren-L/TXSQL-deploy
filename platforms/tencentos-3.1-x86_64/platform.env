# Platform: tencentos-3.1-x86_64
# Status: UNVERIFIED — fingerprints not collected, no target system available.
# Date: 2026-07-22

PLATFORM_ID=tencentos-3.1-x86_64
OS_ID=tencentos
OS_VERSION_ID=3.1
OS_ARCH=x86_64
OS_PRETTY="TencentOS Server 3.1"

# NOTE: Actual OS ID might be "tlinux" not "tencentos".
# TencentOS 3.1 is based on CentOS 8.x / RHEL 8.

# Key questions to resolve:
# - Does it use standard OpenSSL or Tencentsm?
# - Does DNF modularity affect dependency resolution?
# - What glibc version? (likely 2.28)
# - GCC version? (likely 8.x system default)
# - Kernel version? (TLinux 5.x)
# - RPM version? (likely 4.14.x)
# - systemd version? (likely 239+)
# - Does it have yum compat or dnf only?

# Package manager (TBD):
# PKG_MGR=dnf (likely, since RHEL 8 based)
# CREATEREPO_CMD=createrepo_c (likely)
