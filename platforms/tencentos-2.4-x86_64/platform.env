# Platform: tencentos-2.4-x86_64
# Status: UNVERIFIED — fingerprints not collected, no target system available.
# Date: 2026-07-22

PLATFORM_ID=tencentos-2.4-x86_64
OS_ID=tencentos
OS_VERSION_ID=2.4
OS_ARCH=x86_64
OS_PRETTY="TencentOS Server 2.4"

# NOTE: Actual OS ID might be "tlinux" not "tencentos".
# Must verify from /etc/os-release and tencentos-release RPM.
# TencentOS 2.4 is based on CentOS 7.x / RHEL 7.

# Key questions to resolve:
# - Does it use standard OpenSSL or Tencentsm?
# - Is yum or dnf the default package manager?
# - What is the exact glibc version?
# - What kernel version? (TLinux-customized)
# - RPM version?
# - systemd version?
# - Default SELinux state?

# Package manager (TBD):
# PKG_MGR=yum (likely, since RHEL 7 based)
# Or PKG_MGR=dnf (unlikely for RHEL 7 base)
